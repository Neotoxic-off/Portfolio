# GitHub username to source the portfolio from
GITHUB_USER=Neotoxic-off

# Optional GitHub personal access token (read-only / no scopes needed).
# Without it the public API is limited to 60 requests/hour.
GITHUB_TOKEN=
